// Minimal stub to satisfy WalletConnect manifest import checks.
// The real WalletConnect manifest path is not available in this repo's npm registry,
// so this stub preserves the import side effect used by the adapter.
export {};